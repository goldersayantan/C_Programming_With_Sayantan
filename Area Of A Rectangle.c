// Area Of A Rectangle...
#include<stdio.h>
int main()  {
    float length, width;
    printf("Enter the length of a rectangle:");
    scanf("%f",&length);
    printf("Enter the width of a rectangle:");
    scanf("%f",&width);
    printf("The area of a rectangle is: %f",((float) length * width));
return 0;
}